apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: n8n
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`n8n.werrobots.cloud`)
      kind: Rule
      services:
        - name: n8n
          port: 80
